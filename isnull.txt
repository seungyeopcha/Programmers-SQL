SELECT animal_id
from animal_ins
where name is null
order by animal_id;

SELECT animal_id
from animal_ins
where name is not null
order by animal_id;

SELECT animal_type, ifnull(name,'No name'), sex_upon_intake
from animal_ins
order by animal_id;